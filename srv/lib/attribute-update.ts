/**
 * Dynamic guide-attribute updates (telemetry encoding, as-of reconstruction).
 * Extracted to @odatano/dpp-sdk; this shim keeps the historical import path
 * stable.
 */
export * from '@odatano/dpp-sdk/battery/attributes';
