/**
 * BatteryPass-Ready guide document builder. Extracted to @odatano/dpp-sdk;
 * this shim keeps the historical import path stable. Keep the SDK module in
 * sync with scripts/bp-ready-payload.mjs (standalone twin).
 */
export * from '@odatano/dpp-sdk/battery/guide-document';
