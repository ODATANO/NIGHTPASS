/**
 * ZK set-membership claim catalogs. Extracted to @odatano/dpp-sdk; this shim
 * keeps the historical import path stable (the connector bundle and the demo
 * import through it).
 */
export * from '@odatano/dpp-sdk/battery/claim-sets';
